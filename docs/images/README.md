# Visuels ReviewFlow

Ce dossier regroupe les captures affichees dans le README du portfolio.

Les visuels conserves sont :

- `hero-reviewflow.png` : variante de presentation de la fiche structuree ;
- `capture-produit1.png` : page produit et informations extraites ;
- `capture-produit2.png` : variante de capture produit conservee en reserve ;
- `edition-fiche.png` : formulaire de correction editoriale ;
- `fiche-structuree-exports.png` : resultat structure et exports ;
- `fiches-recentes.png` : persistance locale et reprise d'une fiche.

Avant publication, verifier qu'aucune donnee privee, aucun onglet sensible et aucune cle n'apparaissent dans les captures.

`hero-reviewflow.png` est une mise en scene visuelle creee a partir d'une capture reelle. Les autres fichiers sont des captures directes et restent les references fonctionnelles du projet.
