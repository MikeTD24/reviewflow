{
  "name": "@reviewflow/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "npm run clean && tsc --project tsconfig.json && node scripts/copy-static.mjs",
    "test": "vitest run tests --passWithNoTests"
  }
}
